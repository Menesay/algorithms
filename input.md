
```python
int_n = int(input())
int_arr = list(map(int, input().split()))
```

```cpp
for(int i = 0; i < n-1; i++){
    int x;
    cin >> x;
    sum += x;
}

// vectore doldur
vector<int> v(n);
for(auto &x : v) cin >> x;
```